# Resume Agent

This agent analyzes resume text and returns a skills summary, ATS score, and recommendations for improvement.

## Endpoints

- `GET /health`
- `POST /execute`

## Example payload

```json
{
  "text": "Experienced Python developer with FastAPI, SQL, and cloud skills."
}
```
